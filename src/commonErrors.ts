// import AppError from './appError';


// export function notFound(message: string, originalMessage?: string) {
//   let err = new AppError(message, 404, originalMessage);
//   return err;
// }

// export function badRequest(message: string, originalMessage?: string, code?: number, errors?: any) {
//   let err = new AppError(message, 400, originalMessage, code, errors);
//   return err;
// }


// export function unauthorized(message: string, originalMessage?: string) {
//   let err = new AppError(message, 401, originalMessage);
//   return err;
// }